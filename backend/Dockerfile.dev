FROM maven:3.9.3-eclipse-temurin-17
WORKDIR /app

RUN apt-get update && apt-get install -y entr

COPY pom.xml .
RUN mvn dependency:go-offline -B

COPY src ./src
EXPOSE 8080

CMD find src/main | entr -n -r mvn spring-boot:run -Duser.timezone=Asia/Kolkata