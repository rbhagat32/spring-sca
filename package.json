{
  "scripts": {
    "dev": "docker compose -f compose-dev.yaml up --scale backend=5 --watch",
    "prod": "docker compose -f compose.yaml up --scale backend=5 -d --build",
    "down": "docker compose down --rmi local --remove-orphans && docker image prune -f && docker volume rm spring-sca_redis-dump"
  }
}
